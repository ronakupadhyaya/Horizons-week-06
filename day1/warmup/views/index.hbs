<h1>Some random movies</h1>

<table class="table table-striped table-bordered">
  <!-- YOUR CODE HERE -->
  <!-- Below this line is example data, delete it -->
  {{#each data}}
  {{#if @first}}
  <tr>
  {{#each this}}
    <th>
      {{this}}
    </th>
    {{/each}}
  </tr>
  {{else}}
  <tr>
  {{#each this}}
    <td>
      {{this}}
    </td>
    {{/each}}
  </tr>
  {{/if}}
  {{/each}}
</table>
