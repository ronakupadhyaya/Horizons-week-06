import React from 'react';
import {Link} from 'react-router-dom';
import {Route} from 'react-router-dom';

const Start = ({}) => {
    let input;



    return (
			<div>
				<form onSubmit={}>
					<input type="text"
						value={''}
						ref={node => {input = node;}}
						onChange={() =>  }
					/>
				</form>

			</div>
		);
	}
